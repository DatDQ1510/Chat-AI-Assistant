# Production environment variables
# Nginx sẽ proxy /v1/api/ -> http://backend:5000/v1/api/
# Nginx sẽ proxy /socket.io/ -> http://backend:5000/socket.io/
# Frontend chỉ cần gọi relative path
VITE_API_URL=
